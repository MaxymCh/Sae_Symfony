{% extends 'layouts/base-entite.html.twig' %}
 
{% block title %}Nouveau Questionnaire{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Créer Questionnaire</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_questionnaire_index') }}"> 
                    Voir tous les questionnaires
                </a>
            </div>
            <div class="card-body">
                 {{ include('questionnaire/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}