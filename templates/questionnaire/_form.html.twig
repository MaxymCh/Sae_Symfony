
{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.questionnairename, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.questionnairedescription, {'attr': {'class': 'form-control'}}) }}
    </div>
    <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Sauvegarder Questionnaire') }}</button>
{{ form_end(form) }}