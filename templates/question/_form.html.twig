{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.questiontext, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.questiontype, {'attr': {'class': 'form-control'}}) }}
    </div>

    <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Enregistrer Question') }}</button>
{{ form_end(form) }}