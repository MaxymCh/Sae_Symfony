<form method="post" action="{{ path('app_resultat_delete', {'resultatid': resultat.resultatid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat.resultatid) }}">
    <button class="btn">Delete</button>
</form>
